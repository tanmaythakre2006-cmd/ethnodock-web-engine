{
  "name": "cors-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker CORS Proxy",
  "main": "src/index.ts",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.57.1"
  }
}
